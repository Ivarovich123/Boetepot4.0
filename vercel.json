{
  "buildCommand": "cd boetepot-react && npm install && npm install -g typescript && npm run build",
  "outputDirectory": "boetepot-react/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
